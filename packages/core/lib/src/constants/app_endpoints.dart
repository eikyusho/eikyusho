final class AppEndpoints {
  const AppEndpoints._();

  static const String source = 'https://raw.githubusercontent.com/noahbpaiva';

  // * || API
  static const String baseUrl = '$source/eikyusho-extensions-mock/main';
  static const String extensionsList = '/index.min.json';
}
